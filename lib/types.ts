export type MovieType = {
  text: string;
  image: string[];
};
export type GenreType = {
  text: string;
};

export type Postertype = {
  title: string;
  releaseDate: string;
  ratingCode: string;
  duration: string;
  rating: number;
  votes: string;
  p: string;
  director: string;
  writes: string;
  stars: string;
};
